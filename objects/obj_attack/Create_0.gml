// Attack Effect - Create Event
image_speed = 0.3;  // Animation speed
image_index = 0;    // Start at first frame

// Destroy after animation completes
// Adjust this number based on your sprite's frame count
alarm[0] = 20;      // Destroy after 20 steps (about 0.33 seconds at 60fps)